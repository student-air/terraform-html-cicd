name: CI-CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: student972
        password: dckr_pat_Cxxh1KwZJCxr4rK4Pbeh4ExhteU

    - name: Build Docker image
      run: |
        docker build -t student972/html-cicd:latest .

    - name: Push Docker image
      run: |
        docker push student972/html-cicd:latest
