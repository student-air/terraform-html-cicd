name: CI-CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        # These names must match the Secrets names in GitHub Settings exactly
        username: student972
        password: dckr_pat_3xPjxSndtL37o3txhlrlqyb-_Ck

    - name: Build and Push Docker image
      run: |
        docker build -t student972/html-cicd:latest .
        docker push student972/html-cicd:latest
